version: '3.8'

services:
  web:
    build: .
    ports:
      - '3000:3000'
    env_file:
      - .env
    volumes:
      - db-data:/app/prisma
      - uploads:/app/uploads
    restart: unless-stopped

volumes:
  db-data:
  uploads:
